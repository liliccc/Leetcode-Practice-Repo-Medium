package algorithms.level12BFSReview;

class Position {
    int x;
    int y;
    int len;

    public Position(int x, int y, int len) {
        this.x = x;
        this.y = y;
        this.len = len;
    }
}
